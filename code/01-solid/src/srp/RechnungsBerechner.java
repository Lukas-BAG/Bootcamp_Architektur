package srp;

/**
 * AUFGABE SRP (Teil 1/3) — Verantwortlichkeit "Berechnen".
 * ---------------------------------------------------------------
 * Diese Klasse uebernimmt GENAU EINE der drei Aufgaben aus
 * RechnungVorher: die Berechnung des Gesamtbetrags. Sie hat damit
 * nur noch einen einzigen Grund, sich zu aendern - naemlich wenn
 * sich die Berechnungsregeln aendern (z.B. weil eine Steuer dazu
 * kommt). Druck-Layout oder Speicherformat betreffen diese Klasse
 * nicht mehr.
 *
 * TODO: Implementiere berechneGesamtbetrag(...) so, dass die Summe
 *       aller Positionen der uebergebenen Rechnung zurueckgegeben
 *       wird. Vergleiche zum Verhalten RechnungVorher.berechneGesamtbetrag().
 */
public class RechnungsBerechner {

    public double berechneGesamtbetrag(Rechnung rechnung) {
        // TODO: Summe aller rechnung.getPositionen() bilden und zurueckgeben.
        return 0.0; // Sicherer Platzhalter, damit das Projekt schon vor der Loesung compiliert und laeuft.
    }
}
